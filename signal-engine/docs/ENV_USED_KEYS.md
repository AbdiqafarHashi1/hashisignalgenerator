# ENV Used Keys

## core
- `ACCOUNT_SIZE` (default `None`) — Config field `account_size`
- `ADX_PERIOD` (default `14`) — Config field `adx_period`
- `ADX_THRESHOLD` (default `20.0`) — Config field `adx_threshold`
- `ASSET_CLASS` (default `crypto`) — Config field `asset_class`
- `ATR_PERIOD` (default `14`) — Config field `atr_period`
- `ATR_SCORE_MAX_BOOST` (default `15`) — Config field `atr_score_max_boost`
- `ATR_SCORE_SCALE` (default `3000.0`) — Config field `atr_score_scale`
- `ATR_SMA_PERIOD` (default `20`) — Config field `atr_sma_period`
- `BE_MIN_SECONDS_OPEN` (default `60`) — Config field `move_to_breakeven_min_seconds_open`
- `BREAKOUT_ATR_MULTIPLIER` (default `1.2`) — Config field `breakout_atr_multiplier`
- `BREAKOUT_TP_MULTIPLIER` (default `1.2`) — Config field `breakout_tp_multiplier`
- `BREAKOUT_VOLUME_MULTIPLIER` (default `1.5`) — Config field `breakout_volume_multiplier`
- `CANDLE_INTERVAL` (default `None`) — Config field `candle_interval`
- `CURRENT_MODE` (default `SWING`) — Config field `current_mode`
- `DAILY_PROFIT_TARGET_PCT` (default `None`) — Config field `daily_profit_target_pct`
- `DATABASE_URL` (default `None`) — Config field `database_url`
- `DATA_DIR` (default `data`) — Config field `data_dir`
- `DEV_ATR_MULT` (default `1.0`) — Config field `dev_atr_mult`
- `DISABLE_BREAKOUT_CHASE` (default `False`) — Config field `disable_breakout_chase`
- `EMA_LENGTH` (default `50`) — Config field `ema_length`
- `EMA_PULLBACK_PCT` (default `0.0015`) — Config field `ema_pullback_pct`
- `ENGINE_MODE` (default `signal_only`) — Config field `engine_mode`
- `FORCE_TRADE_AUTO_CLOSE_SECONDS` (default `0`) — Config field `force_trade_auto_close_seconds`
- `FORCE_TRADE_COOLDOWN_SECONDS` (default `0`) — Config field `force_trade_cooldown_seconds`
- `FORCE_TRADE_EVERY_SECONDS` (default `5`) — Config field `force_trade_every_seconds`
- `FORCE_TRADE_MODE` (default `False`) — Config field `force_trade_mode`
- `FORCE_TRADE_RANDOM_DIRECTION` (default `True`) — Config field `force_trade_random_direction`
- `FUNDING_BLOCK_BEFORE_MINUTES` (default `10`) — Config field `funding_block_before_minutes`
- `FUNDING_CLOSE_BEFORE_MINUTES` (default `2`) — Config field `funding_close_before_minutes`
- `FUNDING_ELEVATED_ABS` (default `0.02`) — Config field `funding_elevated_abs`
- `FUNDING_EXTREME_ABS` (default `0.03`) — Config field `funding_extreme_abs`
- `FUNDING_GUARD_TAIL_MINUTE` (default `0`) — Config field `funding_guard_tail_minute`
- `FUNDING_INTERVAL_MINUTES` (default `480`) — Config field `funding_interval_minutes`
- `GLOBAL_DD_LIMIT_PCT` (default `0.08`) — Config field `global_drawdown_limit_pct`
- `HTF_BIAS_ENABLED` (default `False`) — Config field `htf_bias_enabled`
- `HTF_BIAS_REQUIRE_SLOPE` (default `False`) — Config field `htf_bias_require_slope`
- `HTF_EMA_FAST` (default `50`) — Config field `htf_ema_fast`
- `HTF_EMA_SLOW` (default `200`) — Config field `htf_ema_slow`
- `HTF_INTERVAL` (default `1h`) — Config field `htf_interval`
- `LEVERAGE_ELEVATED` (default `2.5`) — Config field `leverage_elevated`
- `LEVERAGE_EXTREME` (default `3.0`) — Config field `leverage_extreme`
- `MANUAL_KILL_SWITCH` (default `False`) — Config field `manual_kill_switch`
- `MARKET_DATA_BACKOFF_BASE_MS` (default `500`) — Config field `market_data_backoff_base_ms`
- `MARKET_DATA_BACKOFF_MAX_MS` (default `15000`) — Config field `market_data_backoff_max_ms`
- `MARKET_DATA_FAILOVER_THRESHOLD` (default `3`) — Config field `market_data_failover_threshold`
- `MARKET_DATA_FALLBACKS` (default `binance,okx,replay`) — Config field `market_data_fallbacks`
- `MAX_CONSECUTIVE_LOSSES` (default `None`) — Config field `max_consecutive_losses`
- `MAX_LOSSES_PER_DAY` (default `3`) — Config field `max_losses_per_day`
- `MAX_NOTIONAL_ACCOUNT_MULTIPLIER` (default `3.0`) — Config field `max_notional_account_multiplier`
- `MAX_OPEN_POSITIONS_PER_DIRECTION` (default `0`) — Config field `max_open_positions_per_direction`
- `MAX_STOP_PCT` (default `0.0025`) — Config field `max_stop_pct`
- `MIN_BREAKOUT_WINDOW` (default `25`) — Config field `min_breakout_window`
- `MODE` (default `prop_cfd`) — Config field `MODE`
- `MOMENTUM_MODE` (default `adx`) — Config field `momentum_mode`
- `MOVE_TO_BREAKEVEN_TRIGGER_R` (default `0.6`) — Config field `move_to_breakeven_trigger_r`
- `OI_SPIKE_PCT` (default `0.18`) — Config field `oi_spike_pct`
- `POSITION_SIZE_USD_CAP` (default `None`) — Config field `position_size_usd_cap`
- `PROFILE` (default `profit`) — Config field `PROFILE`
- `PULLBACK_ATR_MULT` (default `0.5`) — Config field `pullback_atr_mult`
- `RANGE_BASE_SCORE_BOOST` (default `8`) — Config field `range_base_score_boost`
- `RANGE_RSI_LONG_MAX` (default `35.0`) — Config field `range_rsi_long_max`
- `RANGE_RSI_SHORT_MIN` (default `65.0`) — Config field `range_rsi_short_min`
- `RANGE_SL_ATR_MULT` (default `0.8`) — Config field `range_sl_atr_mult`
- `RANGE_TP_ATR_MULT` (default `1.0`) — Config field `range_tp_atr_mult`
- `REENTRY_COOLDOWN_MINUTES` (default `30`) — Config field `reentry_cooldown_minutes`
- `RISK_PER_TRADE_USD` (default `None`) — Config field `risk_per_trade_usd`
- `RISK_REDUCTION_ENABLED` (default `True`) — Config field `risk_reduction_enabled`
- `RISK_REDUCTION_TARGET_R` (default `0.7`) — Config field `risk_reduction_target_r`
- `RISK_REDUCTION_TRIGGER_R` (default `0.6`) — Config field `risk_reduction_trigger_r`
- `SCALP_ATR_PCT_MAX` (default `0.012`) — Config field `scalp_atr_pct_max`
- `SCALP_ATR_PCT_MIN` (default `0.0015`) — Config field `scalp_atr_pct_min`
- `SCALP_ATR_PERIOD` (default `14`) — Config field `scalp_atr_period`
- `SCALP_BREAKOUT_LOOKBACK` (default `20`) — Config field `scalp_breakout_lookback`
- `SCALP_EMA_FAST` (default `20`) — Config field `scalp_ema_fast`
- `SCALP_EMA_SLOW` (default `50`) — Config field `scalp_ema_slow`
- `SCALP_EMA_TREND` (default `200`) — Config field `scalp_ema_trend`
- `SCALP_ENGULFING_MIN_BODY_PCT` (default `0.0006`) — Config field `scalp_engulfing_min_body_pct`
- `SCALP_MIN_SCORE` (default `78`) — Config field `scalp_min_score`
- `SCALP_PULLBACK_EMA` (default `20`) — Config field `scalp_pullback_ema`
- `SCALP_PULLBACK_MAX_DIST_PCT` (default `0.002`) — Config field `scalp_pullback_max_dist_pct`
- `SCALP_PULLBACK_MIN_DIST_PCT` (default `0.0`) — Config field `scalp_pullback_min_dist_pct`
- `SCALP_REENTRY_COOLDOWN_MINUTES` (default `20`) — Config field `scalp_reentry_cooldown_minutes`
- `SCALP_REGIME_ENABLED` (default `True`) — Config field `scalp_regime_enabled`
- `SCALP_RETEST_MAX_BARS` (default `6`) — Config field `scalp_retest_max_bars`
- `SCALP_RSI_CONFIRM` (default `True`) — Config field `scalp_rsi_confirm`
- `SCALP_RSI_LONG_MAX` (default `100`) — Config field `scalp_rsi_long_max`
- `SCALP_RSI_LONG_MIN` (default `45`) — Config field `scalp_rsi_long_min`
- `SCALP_RSI_PERIOD` (default `14`) — Config field `scalp_rsi_period`
- `SCALP_RSI_SHORT_MAX` (default `55`) — Config field `scalp_rsi_short_max`
- `SCALP_RSI_SHORT_MIN` (default `0`) — Config field `scalp_rsi_short_min`
- `SCALP_SETUP_MODE` (default `pullback_engulfing`) — Config field `scalp_setup_mode`
- `SCALP_SL_PCT` (default `0.005`) — Config field `scalp_sl_pct`
- `SCALP_TP_PCT` (default `0.01`) — Config field `scalp_tp_pct`
- `SCALP_TREND_FILTER_ENABLED` (default `True`) — Config field `scalp_trend_filter_enabled`
- `SCALP_TREND_SLOPE_MIN` (default `0.0002`) — Config field `scalp_trend_slope_min`
- `SETUP_MIN_CANDLES` (default `3`) — Config field `setup_min_candles`
- `SL_ATR_MULT` (default `1.4`) — Config field `sl_atr_mult`
- `SMOKE_TEST_FORCE_TRADE` (default `False`) — Config field `smoke_test_force_trade`
- `STRATEGY` (default `scalper`) — Config field `strategy`
- `STRATEGY_ADX_THRESHOLD` (default `22.0`) — Config field `strategy_adx_threshold`
- `STRATEGY_ATR_STOP_MULT` (default `1.6`) — Config field `strategy_atr_stop_mult`
- `STRATEGY_BIAS_EMA_FAST` (default `50`) — Config field `strategy_bias_ema_fast`
- `STRATEGY_BIAS_EMA_SLOW` (default `200`) — Config field `strategy_bias_ema_slow`
- `STRATEGY_MAX_ATR_PCT` (default `0.012`) — Config field `strategy_max_atr_pct`
- `STRATEGY_MAX_STOP_PCT` (default `0.015`) — Config field `strategy_max_stop_pct`
- `STRATEGY_MIN_ATR_PCT` (default `0.0015`) — Config field `strategy_min_atr_pct`
- `STRATEGY_MIN_STOP_PCT` (default `0.002`) — Config field `strategy_min_stop_pct`
- `STRATEGY_PARTIAL_R` (default `1.8`) — Config field `strategy_partial_r`
- `STRATEGY_PROFILE` (default `SCALPER_STABLE`) — Config field `strategy_profile`
- `STRATEGY_SWING_LOOKBACK` (default `30`) — Config field `strategy_swing_lookback`
- `STRATEGY_SWING_STOP_BUFFER_BPS` (default `8.0`) — Config field `strategy_swing_stop_buffer_bps`
- `STRATEGY_TARGET_R` (default `2.2`) — Config field `strategy_target_r`
- `STRATEGY_TREND_SLOPE_THRESHOLD` (default `0.0002`) — Config field `strategy_trend_slope_threshold`
- `SWEET8_BLOCKED_CLOSE_TOTAL` (default `0`) — Config field `sweet8_blocked_close_total`
- `SWEET8_CURRENT_MODE` (default `auto`) — Config field `sweet8_current_mode`
- `SWEET8_ENABLED` (default `False`) — Config field `sweet8_enabled`
- `SYMBOLS` (default `["BTCUSDT"]`) — Config field `symbols`
- `TAKE_PROFIT_PCT` (default `0.003`) — Config field `take_profit_pct`
- `TELEGRAM_BOT_TOKEN` (default `None`) — Config field `telegram_bot_token`
- `TELEGRAM_CHAT_ID` (default `None`) — Config field `telegram_chat_id`
- `TELEGRAM_ENABLED` (default `False`) — Config field `telegram_enabled`
- `TICK_INTERVAL_SECONDS` (default `60`) — Config field `tick_interval_seconds`
- `TP_ATR_MULT` (default `2.2`) — Config field `tp_atr_mult`
- `TREND_BIAS_LOOKBACK` (default `20`) — Config field `trend_bias_lookback`
- `TREND_CONFIRM_SCORE_BOOST` (default `10`) — Config field `trend_confirm_score_boost`
- `TREND_MIN_CANDLES` (default `4`) — Config field `trend_min_candles`
- `TREND_RSI_MIDLINE` (default `50.0`) — Config field `trend_rsi_midline`
- `TREND_SCORE_MAX_BOOST` (default `25`) — Config field `trend_score_max_boost`
- `TREND_SCORE_SLOPE_SCALE` (default `40000.0`) — Config field `trend_score_slope_scale`
- `TREND_STRENGTH_MIN` (default `None`) — Config field `trend_strength_min`
- `TREND_STRENGTH_SCALE` (default `2000.0`) — Config field `trend_strength_scale`
- `TRIGGER_BODY_RATIO_MIN` (default `0.0`) — Config field `trigger_body_ratio_min`
- `TRIGGER_CLOSE_LOCATION_MIN` (default `0.0`) — Config field `trigger_close_location_min`
- `VOLUME_CONFIRM_ENABLED` (default `True`) — Config field `volume_confirm_enabled`
- `VOLUME_CONFIRM_MULTIPLIER` (default `1.0`) — Config field `volume_confirm_multiplier`
- `VOLUME_SMA_PERIOD` (default `20`) — Config field `volume_sma_period`
- `WARMUP_IGNORE_HTF_IF_DISABLED` (default `True`) — Config field `warmup_ignore_htf_if_disabled`
- `WARMUP_MIN_BARS_1H` (default `220`) — Config field `warmup_min_bars_1h`
- `WARMUP_MIN_BARS_5M` (default `250`) — Config field `warmup_min_bars_5m`
- `WARMUP_REQUIRE_REPLAY_READY` (default `True`) — Config field `warmup_require_replay_ready`

## replay
- `MARKET_DATA_REPLAY_PATH` (default `data/replay`) — Config field `market_data_replay_path`
- `MARKET_DATA_REPLAY_SPEED` (default `1.0`) — Config field `market_data_replay_speed`
- `REPLAY_END_TS` (default `None`) — Config field `replay_end_ts`
- `REPLAY_RESUME` (default `False`) — Config field `replay_resume`
- `REPLAY_SEED` (default `None`) — Config field `replay_seed`
- `REPLAY_START_TS` (default `None`) — Config field `replay_start_ts`
- `RUN_MODE` (default `live`) — Config field `run_mode`

## risk
- `BASE_RISK_PCT` (default `None`) — Config field `base_risk_pct`
- `COOLDOWN_MINUTES_AFTER_LOSS` (default `None`) — Config field `cooldown_minutes_after_loss`
- `MAX_DAILY_LOSS_PCT` (default `None`) — Config field `max_daily_loss_pct`
- `MAX_RISK_PCT` (default `None`) — Config field `max_risk_pct`
- `MAX_TRADES_PER_DAY` (default `None`) — Config field `max_trades_per_day`
- `MIN_SIGNAL_SCORE` (default `None`) — Config field `min_signal_score`
- `MIN_SIGNAL_SCORE_RANGE` (default `70`) — Config field `min_signal_score_range`
- `MIN_SIGNAL_SCORE_TREND` (default `78`) — Config field `min_signal_score_trend`
- `PROP_DAILY_STOP_AFTER_LOSSES` (default `2`) — Config field `prop_daily_stop_after_losses`
- `PROP_ENABLED` (default `True`) — Config field `prop_enabled`
- `PROP_GOVERNOR_ENABLED` (default `True`) — Config field `prop_governor_enabled`
- `PROP_MAX_CONSEC_LOSSES` (default `2`) — Config field `prop_max_consec_losses`
- `PROP_MAX_TRADES_PER_DAY` (default `4`) — Config field `prop_max_trades_per_day`
- `PROP_PROFIT_TARGET_PCT` (default `0.08`) — Config field `prop_profit_target_pct`
- `PROP_RISK_BASE_PCT` (default `0.0015`) — Config field `prop_risk_base_pct`
- `PROP_RISK_MAX_PCT` (default `0.002`) — Config field `prop_risk_max_pct`
- `PROP_RISK_MIN_PCT` (default `0.0007`) — Config field `prop_risk_min_pct`

## exchange
- `BYBIT_REST_BASE` (default `https://api.bybit.com`) — Config field `bybit_rest_base`
- `BYBIT_TESTNET` (default `False`) — Config field `bybit_testnet`
- `FEE_BPS` (default `5.5`) — Config field `fee_rate_bps`
- `MARKET_DATA_PROVIDER` (default `bybit`) — Config field `market_data_provider`
- `MARKET_PROVIDER` (default `bybit`) — Config field `market_provider`
- `OANDA_INSTRUMENTS` (default `[]`) — Config field `oanda_instruments`
- `SLIPPAGE_BPS` (default `1.5`) — Config field `slippage_bps`
- `SPREAD_BPS` (default `1.5`) — Config field `spread_bps`

## forex
- _None_

## dashboard
- `DEBUG_DISABLE_HARD_RISK_GATES` (default `False`) — Config field `debug_disable_hard_risk_gates`
- `DEBUG_LOOSEN` (default `False`) — Config field `debug_loosen`
- `DEBUG_REPLAY_TRACE` (default `None`) — Direct environment lookup
- `DEBUG_REPLAY_TRACE_EVERY` (default `None`) — Direct environment lookup
- `DEBUG_RUNTIME_DIAG` (default `None`) — Direct environment lookup
